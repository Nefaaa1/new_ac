<?php

namespace App\Support\Search\Sources;

use App\Support\Search\SearchResult;
use App\Support\Search\SearchSource;

/**
 * Recherche dans les contrats.
 *
 * STUB — le modèle Contrat n'existe pas encore : renvoie []. Quand il existera,
 * implémenter ici comme dans ClientSource (icône suggérée : 'file-text').
 */
class ContratSource implements SearchSource
{
    public function search(string $term): array
    {
        return [];
    }
}
